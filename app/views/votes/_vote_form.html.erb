<%# byebug %>
<% if vote.try(:account).nil? %>
  <%= render partial: "votes", locals: { vote: vote, accounts: accounts } %>
  <%= button_to "vote", { action: 'create' }, remote: true %>
<% else %>
  Please enter your payment information to vote for:<br>
  <%# byebug %>
  <%= votee.try(:first_name) %> <%= votee.try(:last_name) %><br>
  <%= render partial: "shared/payer", locals: { payer: payer } %>
  <%= button_to "back", method: :get, action: 'index', validate: false, remote: true %>
<% end %>
